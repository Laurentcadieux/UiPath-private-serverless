# 2026-07-20

- Added Oh My Codex into OpenClaw: active OMX skills registered under `skills/`, native OMX agents installed into `/home/work10/.openclaw/agents/main/agent/codex-home/agents`, and `omx doctor` passed against the OpenClaw Codex home.
- Created first project `UiPath-private-serverless` at `/home/work10/.openclaw/workspace/UiPath-private-serverless` with `src/`, `deploy/`, `docs/`, `tests/`, README, and OMX `AGENTS.md` scaffolding.
- Committed scoped setup changes as `23ccf50` (`Add UiPath private serverless project and OMX skills`). Left unrelated untracked workspace files alone.
- Implemented the `UiPath-private-serverless` MVP as a Python CLI (`uipath-runtime`) for local UiPath Linux Robot container provisioning: config/secret validation, Docker SDK wrapper, local-image-only checks, optional CA/derived image support, package feed/cache setup, deterministic naming, labels, reconciliation, status, logging, docs, scripts, systemd unit, and 20 passing unit tests.
- Live-tested `uipath-runtime` on `67.205.173.227`: cloned/copied current project, installed Docker/dependencies, pulled `uipathprod.azurecr.io/robot/uiautomation-runtime:latest24.10`, initialized 1 robot, scaled to 5 connected robots, verified idempotency, then reduced back to 1.
- Added idle scale logic: config now has `scaling.minimum_count`, `burst_max_count`, `idle_minutes_before_stop`, `poll_interval_seconds`, and an in-container active-job probe. Commands `scale-check` and `scale-watch` report active/idle state and only stop excess idle containers with `--apply`.

## UiPath Runtime Provisioner Continuation Notes

- Project path: `/home/work10/.openclaw/workspace/UiPath-private-serverless`. Live-host copy path: `/opt/uipath-private-serverless-repo/UiPath-private-serverless`.
- Local CLI package command is `uipath-runtime`; mandatory commands implemented include `init`, `status`, `scale-check`, and `scale-watch`.
- Local non-secret test config exists at `UiPath-private-serverless/config/config.test.yaml`; local secret file exists at `UiPath-private-serverless/.secrets/uipath-runtime.secrets.env` and is intentionally gitignored. Do not print or commit machine keys.
- Runtime config on tested hosts lives at `/etc/uipath-runtime/config.yaml`; root-only secret env file lives at `/etc/uipath-runtime/secrets.env`.
- Public UiPath Linux Robot image used in live tests: `uipathprod.azurecr.io/robot/uiautomation-runtime:latest24.10`. The tool still preserves the MVP rule: it does not auto-pull images during `init`; image pull/load is a separate operator action.
- Host `159.223.135.251` was reachable over SSH after password correction, prepared with Docker and the tool, and successfully ran one connected `uipath-robot-001`, but it is very small: about 512 MB RAM. Do not scale it beyond 1.
- Host `67.205.173.227` is the healthier live test host: Ubuntu 24.04 amd64, 2 CPU, about 1.9 GiB RAM, about 58 GB disk. It successfully ran 5 connected robot containers, passed idempotency, then was reduced back to one managed container.
- Current intended state on `67.205.173.227`: only `uipath-robot-001` should remain running and connected. Excess managed containers `uipath-robot-002` through `uipath-robot-005` were removed after the scale test.
- Live connection evidence used for `CONNECTED`: logs containing `Successfully connected to Orchestrator`, `HeartbeatV2 Status:OK`, or `StartService Status:OK`. Do not report `CONNECTED` without evidence.
- A live issue was fixed: mounted package/log directories must be recursively owned by UID/GID `1000:1000`, otherwise Robot logs can fail with `UnauthorizedAccessException` on `/home/robotuser/.local/share/UiPath/Logs/internal`.
- Latest local commits related to the scaler/live work include `fc941d5` (`Add idle scale check command`), `fab96fd` (`Add scale watch loop`), and `26a04b0` (`Record UiPath runtime live scale test`). Earlier MVP commit was `c93cdc8` and cache ignore commit was `ddcba88`.
- `README-LIVE-HOST.md` was created/updated with live-host usage instructions. Keep usage docs in the project, not in workspace root.
- GitHub push/auth remained blocked from this environment, so live hosts were updated by copying the current local snapshot over the HTTPS clone. Once GitHub auth is fixed, push local commits and remove the need for overlay copying.
